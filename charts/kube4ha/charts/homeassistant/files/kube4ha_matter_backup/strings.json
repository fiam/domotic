{
  "config": {
    "step": {
      "user": {
        "title": "Include Matter in native backups",
        "description": "Back up the kube4ha Matter Server's device pairings with Home Assistant. Matter briefly reconnects while each snapshot is taken."
      }
    },
    "abort": { "already_configured": "Matter backup is already configured." }
  }
}

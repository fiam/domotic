apiVersion: v1
kind: Service
metadata:
  name: {{ include "domotic.fullname" . }}
  labels:
    {{- include "domotic.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "domotic.selectorLabels" . | nindent 4 }}
